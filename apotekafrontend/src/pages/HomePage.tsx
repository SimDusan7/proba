import { Grid, Typography } from "@mui/material";

const HomePage = () => {
    return(<div>
        <Grid container display="flex" justifyContent="center" alignItems="center">
            <Grid item>
                <Typography variant="h5">Dobro dosli u Apoteka aplikaciju.</Typography>
            </Grid>
        </Grid>
    </div>)
}

export default HomePage;
